# Changelog

本文档记录 KaiTab 的版本迭代（语义化版本，SemVer）。

## [1.1.0] - 2026-09-14

### 新增
- **常用站点磁贴**（模式页顶部的 `Shortcuts` 区）：按浏览习惯自动生成常用站点，支持手动添加 / 修改 / 移除与拖动排序；自动项与手动项各有独立名额，互不挤占。
  - 数据取自浏览器本机的「最常访问」记录，走**可选权限、按需授权**；未授权时不读取任何数据。
  - 站点图标取自浏览器**本机缓存**，不联网、不依赖任何第三方图标服务；取不到时回退为字母色块。
- 顶栏品牌区：产品名与源码仓库入口。

### 变更
- 标签页管理模式的显示名更新为 **Mission**；功能与数据不变，原有设置与备份继续可用。
- 权限说明：新增 `favicon`（因已声明 `tabs`，**不产生额外权限警告**）；`topSites` 为可选权限，仅在使用「常用站点磁贴」时需要。

### 修复
- 模式页重复标签提示条的文案随显示名同步。

## [1.0.0] - 2026-09-03

首个稳定版（first stable release）。

### 功能
- 壳扩展唯一接管 `chrome://newtab`，内部集成多模式、一键切换、记忆上次选择。
- 模式：WeTab 网页版仪表盘（iframe 嵌入）。
- 模式：Tab Out 标签管理器（本地移植自开源项目），支持按域名分组、重复检测、一键关闭、稍后阅读。
- Tab Out 自定义分组：基于规则的命名分组，规则存于 `chrome.storage.local`，随备份导出自动覆盖。
- 全局快捷键 `Ctrl+Shift+1..4` 切换模式。
- 配置备份与恢复：全部 KaiTab 配置导出 / 导入为带时间戳的 JSON。
- 关闭全部二次确认：域内 / 全局「Close all」点击弹窗确认，避免误关大量标签。
- 浅色 / 深色主题适配。
